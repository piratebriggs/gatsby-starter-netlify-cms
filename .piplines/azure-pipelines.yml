name: $(date:yyyyMMdd)$(rev:.r)-$(SourceBranchName)

trigger:
- master

pool:
  name: NBS-MC-COUK-SC-DEV-BUILD

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    yarn
    yarn build
  displayName: 'yarn install and build'